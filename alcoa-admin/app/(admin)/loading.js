import { RouteLoadingView } from "@/components/loading/loading-kit";

/** Shown while any admin route segment is loading (RSC / soft navigation). */
export default function AdminRouteLoading() {
  return <RouteLoadingView />;
}
