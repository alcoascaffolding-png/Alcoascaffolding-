/**
 * Amiri Arabic Font for jsPDF
 * Base64 encoded font data (subset for common Arabic characters)
 * This is a minimal subset - for full font, use external file
 */

// This is a placeholder - we'll use a different approach
// jsPDF standard fonts don't support Arabic well
// We need to use the text directly and let the browser/PDF viewer handle it

export const getAmiriFont = () => {
  // Return null to indicate we should use alternative rendering
  return null;
};

export default getAmiriFont;
